# Time Assistant tenant store (template)
Seed for a new customer's private memory repo. Replace config.json placeholders,
keep rules/customers as starting points. Daily/weekly score files are created by
the assistant on first run.
